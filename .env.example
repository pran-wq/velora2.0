# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="AIzaSyDuW8x2P9dz5EDfVR2YG9lBTaFfeZIUTzY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

DATABASE_URL="postgresql://postgres:Rahul%4091410@db.pyerufwcbnwwsszqltbk.supabase.co:5432/postgres"

# --- Aether hackathon / local dev ---
# Server (Express): medical report analyzer + chat proxy
PORT=3001
GEMINI_MODEL=gemini-2.0-flash
# Optional: OpenAI fallback for pasted text when Gemini is unavailable
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Client (Vite): must point to the server for /api/ai/medical-report
VITE_API_URL=http://localhost:3001
# Optional: browser-side Gemini for other features (home summaries, etc.)
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.0-flash
